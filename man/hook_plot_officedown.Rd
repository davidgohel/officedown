% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hooks.R
\name{hook_plot_officedown}
\alias{hook_plot_officedown}
\title{knitr hook for figure caption autonumbering}
\arguments{
\item{x}{Character string containing the path to the saved plot file.}

\item{options}{Named list of chunk options.}
}
\value{
A character string containing markdown with embedded OOXML code that
combines the auto-numbered caption and the Pandoc image syntax.
}
\description{
Plot hook for Word documents that generates auto-numbered figure captions
with custom styling. The hook uses Pandoc markdown syntax for images
combined with OOXML for captions and paragraph formatting.

The hook delegates image rendering to Pandoc while providing enhanced
caption management with auto-numbering, cross-references, and custom
Word paragraph styles.
}
\section{Supported chunk options}{

\itemize{
\item \code{fig.cap}: Figure caption text. Used to generate auto-numbered captions
with cross-reference support.
\item \code{fig.cap.style}: Paragraph style for the caption (default from options).
\item \code{fig.cap.pre}, \code{fig.cap.sep}: Caption prefix and separator for
auto-numbering (e.g., "Figure" and ": ").
\item \code{fig.id}: Cross-reference identifier (defaults to chunk label).
\item \code{fig.topcaption}: Logical; if TRUE, caption appears above the image.
\item \code{out.width}, \code{out.height}: Output dimensions. Can use units like "5in",
"80\%", "10cm". Passed directly to Pandoc.
\item \code{out.extra}: Additional Pandoc image attributes.
\item \code{fig.alt}: Alternative text for the image.
\item \code{fig.align}: Image alignment. Must be one of "default", "left", "center",
or "right". Default value "default" is converted to "center".
\item \code{fig.style}: Word paragraph style name for the image paragraph
(default: "Normal").
}
}

\section{Global options}{

\itemize{
\item \code{fig.cap.tnd}: Caption numbering depth (default: 0).
\item \code{fig.cap.tns}: Caption numbering separator (default: "-").
\item \code{fig.cap.fp_text}: Text formatting properties for caption numbers.
}
}

\examples{
\dontrun{
# Chunk with auto-numbered caption and custom size
```{r, fig.cap="Sales over time", out.width="80\%"}
plot(sales_data)
```

# Chunk with alignment and custom style
```{r, fig.cap="Key findings", fig.align="center", fig.style="ImageCenter"}
plot(results)
```

# Caption above image with cross-reference
```{r sales-plot, fig.cap="Q4 Sales", fig.topcaption=TRUE, fig.id="sales"}
plot(sales_data)
```
}
}
